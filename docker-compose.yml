services:
  smb-mirror:
    image: ghcr.io/alex7755772-lgtm/smb-mirror:latest
    build: .
    container_name: smb-mirror
    restart: unless-stopped
    init: true
    ports:
      - "5800:5800"
    environment:
      TZ: America/Edmonton
      PORT: 5800
      MAX_JOB_RUNTIME_MINUTES: 1440
    volumes:
      - ./config:/app/config
      - ./data:/app/data
      - /mnt:/mnt
